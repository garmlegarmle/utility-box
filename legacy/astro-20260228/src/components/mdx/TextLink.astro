---
interface Props {
  href: string;
  label: string;
  arrow?: boolean;
}

const { href, label, arrow = true } = Astro.props;
const withArrow = arrow && !label.trim().endsWith('→');
---
<a class="text-link" href={href}>
  {label}{withArrow ? ' →' : ''}
</a>
